= form_tag remove_additional_user_path(@ticket), :method => :delete do
  .actions
    = submit_tag "Remove Additional Provider"
    = select :user, :user_id, get_additional_providers_for(@ticket), :include_blank => true
  